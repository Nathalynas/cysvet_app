<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="cysvet-backend"/>
    <springProperty scope="context" name="activeProfiles" source="spring.profiles.active" defaultValue="default"/>

    <property name="CONSOLE_PATTERN"
              value="ts=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} level=%-5level app=${appName} profile=${activeProfiles} requestId=%X{requestId:-} tenantId=%X{tenantId:-} userId=%X{userId:-} method=%X{httpMethod:-} path=%X{httpPath:-} status=%X{httpStatus:-} durationMs=%X{durationMs:-} logger=%logger{36} msg=&quot;%replace(%msg){'&quot;','\\&quot;'}&quot;%n%ex"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
